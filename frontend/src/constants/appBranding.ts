/**
 * Product branding (user-facing only).
 * Not affiliated with Rose & Associates or third-party MMRA® workbook naming.
 */
export const APP_TITLE = 'Stochastic volume calculation'

/**
 * Short descriptor under the app name (sidebar header).
 */
export const APP_TAGLINE = 'Monte Carlo volumetric assessment'

/** Browser tab / export header. */
export const APP_DOCUMENT_TITLE = `${APP_TITLE} — ${APP_TAGLINE}`

/** Neutral phrase for legacy Excel workbook references in help text. */
export const LEGACY_WORKBOOK_LABEL = 'reference Excel workbook'
